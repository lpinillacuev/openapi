name: PR Commands

# How a reviewer decides on pending paths:
#
#   - Click "Review changes" → "Approve"           → all pending paths approved
#   - Click "Review changes" → "Request changes"   → all pending paths rejected
#
# After the review, the bot updates bot/pending-paths.
# When all paths are decided, the check turns green and merge is unblocked.

on:
  pull_request_review:
    types: [submitted]

permissions:
  issues: write
  pull-requests: write

jobs:
  # ── Job 1: GitHub native review (Approve / Request changes) ─────────────────
  handle-review:
    name: Handle GitHub Review decision
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'pull_request_review' &&
      (github.event.review.state == 'approved' ||
       github.event.review.state == 'changes_requested') &&
      (github.event.review.author_association == 'OWNER' ||
       github.event.review.author_association == 'MEMBER' ||
       github.event.review.author_association == 'COLLABORATOR')

    steps:
      - name: Extract PR context
        id: ctx
        run: |
          PR_NUMBER="${{ github.event.pull_request.number }}"
          BRANCH="${{ github.event.pull_request.head.ref }}"
          COMMIT_SHA="${{ github.event.pull_request.head.sha }}"
          REVIEWER="${{ github.event.review.user.login }}"
          REVIEW_STATE="${{ github.event.review.state }}"

          APP=$(echo "$BRANCH" | sed -n 's|enhancement/automatic-spec-\([^/]*\)/.*|\1|p')

          if [ "$REVIEW_STATE" = "approved" ]; then
            ACTION="approve"
          else
            ACTION="reject"
          fi

          echo "action=$ACTION"        >> $GITHUB_OUTPUT
          echo "app=$APP"              >> $GITHUB_OUTPUT
          echo "reviewer=$REVIEWER"    >> $GITHUB_OUTPUT
          echo "pr_number=$PR_NUMBER"  >> $GITHUB_OUTPUT
          echo "commit_sha=$COMMIT_SHA" >> $GITHUB_OUTPUT
          echo "Reviewer $REVIEWER $ACTION for app=$APP"

      - name: Skip if not a sync PR
        if: steps.ctx.outputs.app == ''
        run: |
          echo "Not a sync PR — skipping."
          exit 0

      - name: Parse checkboxes and decide per path
        id: decide
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || github.token }}
        run: |
          ACTION="${{ steps.ctx.outputs.action }}"
          APP="${{ steps.ctx.outputs.app }}"
          REVIEWER="${{ steps.ctx.outputs.reviewer }}"
          PR_NUMBER="${{ steps.ctx.outputs.pr_number }}"
          COMMIT_SHA="${{ steps.ctx.outputs.commit_sha }}"
          BOT_URL="${{ secrets.BOT_SYNC_SPECS_URL || 'https://beta-bot-sync-specs.melioffice.com' }}"

          # Read the PR body
          PR_BODY=$(gh pr view "$PR_NUMBER" --json body --jq '.body' 2>/dev/null || echo "")

          # Extract paths between markers
          SECTION=$(echo "$PR_BODY" | sed -n '/PENDING_PATHS_START/,/PENDING_PATHS_END/p')

          if [ -z "$SECTION" ]; then
            echo "No pending paths section found — nothing to decide."
            echo "decided_count=0" >> $GITHUB_OUTPUT
            exit 0
          fi

          APPROVED=()
          REJECTED=()

          # Parse checked [x] and unchecked [ ] boxes
          while IFS= read -r line; do
            if echo "$line" | grep -qE '^\- \[x\]'; then
              PATH_VAL=$(echo "$line" | sed "s/- \[x\] \`//;s/\`//")
              APPROVED+=("$PATH_VAL")
            elif echo "$line" | grep -qE '^\- \[ \]'; then
              PATH_VAL=$(echo "$line" | sed "s/- \[ \] \`//;s/\`//")
              REJECTED+=("$PATH_VAL")
            fi
          done <<< "$SECTION"

          echo "Approved paths: ${APPROVED[*]}"
          echo "Rejected paths: ${REJECTED[*]}"

          TOTAL=0

          # If reviewer clicked "Approve" — use checkbox state
          # If reviewer clicked "Request changes" — reject ALL pending paths
          if [ "$ACTION" = "reject" ]; then
            # Request changes = reject everything regardless of checkboxes
            ALL_PATHS=("${APPROVED[@]}" "${REJECTED[@]}")
            APPROVED=()
            REJECTED=("${ALL_PATHS[@]}")
          fi

          for PATH_VAL in "${APPROVED[@]}"; do
            PAYLOAD="{\"path\":\"$PATH_VAL\",\"approvedBy\":\"$REVIEWER\",\"decidedBy\":\"$REVIEWER\",\"prNumber\":$PR_NUMBER,\"commitSha\":\"$COMMIT_SHA\"}"
            curl -s -X POST "${BOT_URL}/openapi/policy/${APP}/approve" \
              -H "Content-Type: application/json" \
              -H "X-github-token: ${{ secrets.GH_PAT }}" \
              -d "$PAYLOAD" > /dev/null
            TOTAL=$((TOTAL + 1))
            echo "✅ Approved: $PATH_VAL"
          done

          for PATH_VAL in "${REJECTED[@]}"; do
            PAYLOAD="{\"path\":\"$PATH_VAL\",\"decidedBy\":\"$REVIEWER\",\"reason\":\"rejected via GitHub Review\",\"prNumber\":$PR_NUMBER,\"commitSha\":\"$COMMIT_SHA\"}"
            curl -s -X POST "${BOT_URL}/openapi/policy/${APP}/reject" \
              -H "Content-Type: application/json" \
              -H "X-github-token: ${{ secrets.GH_PAT }}" \
              -d "$PAYLOAD" > /dev/null
            TOTAL=$((TOTAL + 1))
            echo "❌ Rejected: $PATH_VAL"
          done

          echo "decided_count=$TOTAL"        >> $GITHUB_OUTPUT
          echo "approved_count=${#APPROVED[@]}" >> $GITHUB_OUTPUT
          echo "rejected_count=${#REJECTED[@]}" >> $GITHUB_OUTPUT

      - name: Comment result on PR
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || github.token }}
          REVIEWER: ${{ steps.ctx.outputs.reviewer }}
          PR_NUMBER: ${{ steps.ctx.outputs.pr_number }}
          APPROVED: ${{ steps.decide.outputs.approved_count }}
          REJECTED: ${{ steps.decide.outputs.rejected_count }}
        run: |
          APPROVED="${APPROVED:-0}"
          REJECTED="${REJECTED:-0}"
          TOTAL=$((APPROVED + REJECTED))

          if [ "$TOTAL" -eq 0 ]; then
            echo "No decisions to comment."
            exit 0
          fi

          BODY_FILE="/tmp/pr-command-result.md"
          {
            echo "**@${REVIEWER}** reviewed the pending paths:"
            echo ""
            if [ "$APPROVED" -gt 0 ]; then
              echo "✅ **${APPROVED}** path(s) approved (whitelist)"
            fi
            if [ "$REJECTED" -gt 0 ]; then
              echo "❌ **${REJECTED}** path(s) rejected (blacklist)"
            fi
            echo ""
            echo "🎉 PR unblocked and ready to merge."
          } > "$BODY_FILE"

          gh issue comment "$PR_NUMBER" \
            --repo "${{ github.repository }}" \
            --body-file "$BODY_FILE"
